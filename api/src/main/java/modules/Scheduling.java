package modules;

import models.State;

import java.util.Map;

public class Scheduling {

    public Map<State, State[]> useFaultTolerateStrategy(Map<State, State[]> T, Map<State, double[]> R, double threshold){
        //Fault Tolerate Strategy
        return null;
    };
}
